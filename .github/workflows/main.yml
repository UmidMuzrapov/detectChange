name: Echo Changes

on: push

jobs:
  echo-changes:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: List Changes
      run: |
        echo "Changes in the push:"
        git diff ${{ github.event.before }} ${{ github.event.before }}
